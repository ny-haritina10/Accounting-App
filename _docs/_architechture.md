# Project Architechtures

- backend
    + Java Spring Boot
    + REST API

- frontend
    + React 
    + Taiwind CSS
    + ShadCN UI

- sql
    + Postgres
        - add the prefix columns for each tables
        - add the prefix at table creation 
            - prefix VARCHAR(5) DEFAULT 'USR',     
